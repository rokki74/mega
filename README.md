This repository is not really a project but rather a collection of multiple projects rather than being one itself.
For example the projects within this repo are it's directories e.g fql directory contains my project for file query language which works on multimedia and object detections. 
Every project is independent with their own readme and implementations or documentations.
