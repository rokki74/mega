/*
struct Pose(pose_keypoints)
enum Actions{
    Standing,
    Bending,
    Moving,
}

enum relationship{
   Distance("close together"),
   Relative_motion("Motion direction"),
   Pose("Their pose"),
   Duration("stayed together for long"),
}

#[derive(Debug)]
struct Track {
    id: u32,
    class: String,
    bbox: ,
    center: ,
    first_seen: Duration,
    last_seen: Duration,
    history:
}


fn calculate_center(){

}
*/
